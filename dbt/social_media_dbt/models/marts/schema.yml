version: 2

models:
  - name: fct_posts
    description: "Fact model for Facebook posts"
    columns:
      - name: post_id
        description: "Primary key for posts"
        tests:
          - unique
          - not_null

  - name: fct_comments
    description: "Fact model for Facebook comments"
    columns:
      - name: comment_id
        description: "Primary key for comments"
        tests:
          - unique
          - not_null
